// import dotenv from "dotenv";
// dotenv.config();

// V1
// let config = { api_host: process.env.REACT_APP_URL_DEV, api_image: process.env.REACT_APP_IMAGE_AVATAR_DEV };

// V2
let config = { api_host: "http://localhost:4000", api_image: "http://localhost:4000/uploads" };

export { config };
